# ERP-v4：空间形状自适应实验（2026-09-22）

这是一条实验分支，不替换旧版 v3，不预先声称性能提高。用户提出的核心问题成立：在 0/4/7 mm 三种固定模板中选择，并不等于从数据学习活动区形状。

## 本轮已完成结果

固定参数完成20病例×8方法（四场景、五SNR）和12个非高斯形状病例。主复测同病例 v3→v4：总体An_auc 0.9831→0.9208、表层0.9773→0.9893、深层0.9689→0.6311。非高斯挑战AUC上升但Dice下降。**因此本轮实现了自适应权重机制，尚未证明定位性能和形状恢复优于旧版。**

详细结果：`results/erp_whole_head/adaptive_v4/pilot_five_snr_all_methods/REPORT.md` 和 `results/erp_whole_head/adaptive_v4/shape_mrf_final/REPORT.md`。旧版不覆盖，全量9,212例未重跑，真实数据GFP链路未改。

## 实际改了什么

保留原有 ERP 时间基、观测、白化和模态证据加权，只更换空间求解。所有 7,514 个位置同时进入逆问题；不以真实源位置裁剪候选集，不选择高斯模板，也不把额外“证据图”混进最终估计。

记白化后的观测为 Y，前向矩阵为 G，活动期选出的正交时间基为 Q，B=YQ。D 是实际皮层网格的边差分矩阵（21,320 条边）。

初始直接版本令 Z=X；SISSES 启发的邻域版本令：

    P = 去掉自环后的皮层邻接矩阵，按行归一化
    M = I − τP
    Z = MX，A = GM⁻¹

深层块 M=I，不将丘脑点与皮层连接，也不对方向可能相反的丘脑标量电流直接加 TV 边。τ=0 即直接版本。τ 是固定的邻域相关强度，不是每个病例选择的模板半径。

每次外迭代求解：

    min_Z  1/2 ||B − AZ||²_F
           + λ Σ_j a_j w_j ||Z_j||₂
           + β Σ_e v_e ||(DZ)_e||₂

然后更新：

    w_j = ε_a / (||Z_j||₂ + ε_a)
    v_e = ε_e / (||(DZ)_e||₂ + ε_e)

第一轮用全 1 权重。ε 在第一轮之后固定，后续是明确 log-sum 目标的主化最小化，不能每轮重新用不同最大值改变目标。a_j 是固定的前向灵敏度补偿，λ 来自同宽基线片段投影；99% 基线分位是正则化启发式，不是显著性 p 值。β 控制边缘与幅度惩罚的比例。

**严格区别**：τ=0 时更新的是物理源幅度 X 与边缘 DX；τ>0 时更新的是邻域创新量 MX 与 D MX。这是 SISSES 式空间先验的改写，不应该把二者混称为完全相同，也不应宣称是恢复了 PPT 中的原公式。支持范围由联合数据拟合、逐顶点权重和逐边权重共同决定，没有三种高斯半径可选。

ADMM 使用稀疏图矩阵与传感器空间 Woodbury 求解，避免 7,514×7,514 稠密逆矩阵。每例保存内外迭代、目标函数、残差、权重范围及收敛标志。达到预算或触发目标上升保护，不算收敛。

输出使用 X=M⁻¹Z，并以 pinv(B)Y 延拓整段有符号时程。因此再投影到 Q 会还原优化系数；不会在窗口之外人为填零。当前实验入口只接受一个连续活动窗，尚未实现多窗联合空间求解。

## 先行试验：失败结果也保留

- `smoke_01`：第一版四场景，直接幅度/边缘稀疏使表层结果变差，且未充分收敛。
- `spatial_penalty_screen_01`：6 个幅度/边缘强度设置 × 4 场景。单纯增强边缘惩罚没有解决问题，纯浅层中出现深源假阳性。
- `spatial_mrf_screen_01`：τ=0/.5/.8 × 4 场景。τ=.5 恢复了较好的表层排序与位置，但两个混合病例漏掉深源；τ=.8 连纯深病例也可能失败。这些数值不作为最终已收敛性能。
- `shape_initial`：4 个非高斯形状 × 3 SNR，显示原来的直接点稀疏版本仍不足以恢复范围。高 AUC 本身不证明范围正确，应结合 Dice、SD、DLE 与活动点数。

## 仿真边界

原开发数据为 188 个配置 × 49 个 EEG/MEG SNR 组合，共 9,212 例。四种场景为纯表层、纯深层、深+单表层、深+双表层。皮层覆盖双侧 68 个 aparc 分区；深层仅 16 个双侧丘脑点，不代表所有深部脑区。

活动期固定 20–120 ms，仅为隔离空间算法变化，不是对真实数据固定 N20/P30 时间窗的认可。本轮还未实现 GFP 自动选时。

SNR 是相当于 40 次叠加平均后的水平；仿真并没有保存 40 个独立试次。噪声为空间有色高斯，生成与反演使用同一前向矩阵。这些是实验局限，不能据此声称真实数据已解决。

评价沿用 An_auc，并将表层/深层分开；不含某层真值时该层指标未定义。混合源必须同时报告深源检出、假阳性与漏检惩罚距离，不能用大量无源顶点抬高的总体 AUC 遮掩漏深源。

七个固定对比方法：MNE、dSPM、sLORETA、eLORETA、LCMV、Dipole fitting (grid)、RAP-MUSIC。SISSES 是本次空间思想的参考，不是这七个方法中的一个。

## 代码入口

- `作者风格版/14-ERP自适应空间重加权仿真.py`：按 #%% 顺序运行，参数、四场景和多 SNR，默认小规模开发复测。
- `作者风格版/15-ERP空间正则开发诊断.py`：开发参数探索，历史输出不覆盖。
- `作者风格版/16-ERP新版旧版配对结果.py`：同病例 v3/v4 比较及配置聚类区间。
- `candidates/oaster_adaptive.py`：共享数值求解器。保留必要函数，避免在每个实验脚本复制矩阵迭代。
- `run_adaptive_shape_challenge.py`：1-hop、3-hop、细长片及宽片+深源的非高斯补充挑战。

本次正式复测的规模、图表和数值由对应结果目录中的 metadata、completion 和 REPORT 记录；不能把入口能够运行的规模写成已经完成的规模。
