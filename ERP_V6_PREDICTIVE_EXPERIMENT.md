# ERP-v6：先检验是否需要深源，再报告定位

2026-09-22，开发候选。保留 v3/v4/v5、原 An_auc/SD/DLE 和原幅度/距离评价阈值；不声称本候选已通过验收。

## 为什么不能只调深源幅度阈值

已有 v5 非高斯开发结果中，9 个纯表层病例有 7 个深/全幅度比恰好为 1。阈值不超过 1 时这 7 个仍误报，超过 1 则所有深源都被拒绝。一个高 SNR 纯表层病例中，假深源时程与真表层相关 0.983，是表层信号误归到深层。详见 `results/erp_whole_head/adaptive_v6/development_diagnosis/REPORT.md`。

## 本轮新链路

1. 用同一真值生成两份独立的 20-trial 均值等效观测。噪声尺度只按期望能量确定一次，合并 40-trial 均值具有标称期望 SNR；不按每份实际噪声重新归一化。它们不是保存的 40 个独立 epoch。
2. 白化与 EEG/MEG 权重仅从训练份确定。在全皮层上拟合 H0；在同一皮层加全部 16 个深点上拟合 H1。两者使用源幅度/边差分重加权，没有三个预制空间模板。
3. 在独立确认份上比较固定 10 维 DCT 响应的预测误差。令 B 为确认观测的响应，F0、F1 为训练解的前向预测，V 为确认基线估计的响应噪声能量：

       T = (||B-F0||² - ||B-F1||²) / V

   T>0 只表示加入深源改善预测，不足以证明有深源；可重复的表层拟合残差也能使 T>0。
4. 另冻结 19 个纯表层配置 ×3 SNR 的校准面板，包含单/双表层。每组计算 `p=(1+count(T_null>=T))/(n+1)`，平局保守处理。对所有病例使用同一个规则：三个校准组的 p 都 <=0.05，且 T>0，才允许 H1。**测试真实 SNR 不进入决策。** 拒绝时返回完整重新拟合的 H0，不是仅把 H1 的深点清零。

校准分数和全部参数必须在一次性验证前冻结。小样本固定几何外推不满足随机校准的可交换假设，因此这里只称经验排名校准，不宣称真实 FPR 受 5% 保证。排名校准的理论背景见 [Bates 等，Testing for Outliers with Conformal p-values](https://arxiv.org/abs/2104.08279)。

## 验收不是只看 AUC

固定验证 24 病例，8 源配置 ×3 SNR。每个 SNR 4 个无深源、4 个有深源。存在性误报直接数 `deep_present_decision`，不能再借原 0.14 幅度阈值遮蔽已经报出的深源。原幅度阈值后的定位检出和误报另列。

所有硬门槛同时通过才合格：每 SNR 无深源误报 <=5%（本样本必须0/4）；总体定位检出 >=80%（至少10/12）；检出者原 DLE 均值<=10 mm；含漏检惩罚的深层 DLE 均值<=60 mm；所有 H0/H1 求解均收敛。表层和深层 AUC、SD/DLE仍全部报告。未通过不能靠更改本批测试阈值“修复”。

表层使用新中心，但部分连片与历史活动区域重叠；全部16个深点曾被使用，纯深只能称新噪声验证。固定3组SNR为 EEG/MEG −10/−10、−10/20、20/−10 dB。不是全头所有位置或真实数据验证。

## 文件与当前状态

- `作者风格版/22-浅深检出验收协议.py`：冻结开发/一次性验证 manifest。
- `作者风格版/23-独立观测浅深检出仿真.py`：按 #%% 顺序执行，开发/校准/验证分别保存，不覆盖已有目录。
- `作者风格版/24-纯表层校准病例.py`：冻结57个校准病例。
- `candidates/oaster_predictive.py`：H0/H1 拟合、独立预测评分及排名校准。
- `benchmark/erp_replicates.py`：两份独立观测，训练份确定预处理。
- `benchmark/deep_acceptance.py`：存在性误报、定位检出、距离及数值收敛硬验收。

第一组 IRLS 开发试算（5/5 dB、5病例）已完成，尚不适合确认：两个混合病例 T<0，单表层病例达到100步上限而未收敛。未打开冻结验证集。初始 IRLS 参数为 epsilon_fraction=1、smoothing_fraction=0.1，属于开发选择，非预先独立验证的优良设置；另保留原 ADMM 求解路径用于定位问题与证据问题的区分。

七种比较方法可同时报告 train-20 和 combined-40 条件；v6使用训练20份进行反演、确认20份判断存在性，不能把两种数据使用策略称为完全相同的单份输入。保留完整时程，未把基线人为置零；v5 的时程延拓限制尚未因此自动消除。固定 ERP 窗、同一生成/反演几何和等效 Gaussian 试次仍是限制。
