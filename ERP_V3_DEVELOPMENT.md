# OASTER-ERP v3 开发记录

状态：开发阶段已冻结，尚未查看或运行 `confirmation_v3`。

v3 在 v2 的表层结果基础上，先投影掉已选表层子空间，再用完整时域残差重建时间基、选择一个深层候选，并用同一表层残差计算深层连续证据。算法不读取场景、SNR 或仿真真值。

## 980 例小开发矩阵

| 指标 | v2 | v3 |
|---|---:|---:|
| 全局 An_auc | 0.9707 | 0.9715 |
| 表层 An_auc | 0.9617 | 0.9617 |
| 深层 An_auc | 0.8415 | **0.9004** |
| 深层 An_auc ≥ 0.90 的 SNR 格 | 12/49 | **31/49** |
| 深层敏感度 | 0.6014 | **0.6558** |
| 纯表层特异度（小开发阈值 0.125） | 0.9061 | 0.9020 |
| 深层平衡准确率（小开发阈值 0.125） | 0.7537 | **0.7789** |
| 漏检惩罚后的深层 DLE | 94.08 mm | **81.11 mm** |

分情景深层 An_auc：纯深层 `0.9978 → 0.9962`，一深一浅 `0.7118 → 0.8356`，一深两浅 `0.8150 → 0.8694`。

这里仍不能声称“所有 SNR 都达到 0.90”。最低 EEG/MEG SNR 组合的混合深源仍明显较差；而且这个小矩阵每种情景只有 5 套空间配置，配置聚类后的增益区间仍跨零。

## 9,212 例全位置开发矩阵

| 指标 | v2 | v3 |
|---|---:|---:|
| 全局 An_auc | 0.9641 | **0.9652** |
| 表层 An_auc | 0.9613 | 0.9613 |
| 深层 An_auc | 0.7460 | **0.8357** |
| 深层 An_auc ≥ 0.90 的 SNR 格 | 0/49 | **21/49** |
| 一深一浅的深层 An_auc | 0.7400 | **0.8206** |
| 一深两浅的深层 An_auc | 0.6261 | **0.7856** |
| 纯深层的深层 An_auc | **0.9951** | 0.9875 |
| 深层敏感度 | 0.5041 | **0.5425** |
| 纯表层特异度（开发运行阈值 0.125） | **0.9256** | 0.8986 |
| detection-aware 深层 DLE | 116.82 mm | **107.72 mm** |

按 120 套含深源的位置配置聚类后，v3-v2 深层 An_auc 平均增益为 `+0.0898`，10,000 次配置 bootstrap 的 95% 区间为 `[+0.0674, +0.1120]`。一深一浅和一深两浅的配置级增益分别为 `+0.0807` 和 `+0.1595`；纯深层下降 `-0.0076`，但绝对值仍为 0.9875。

开发集上测试过的 10 mm 表层尺度、分模态残差重加权、软深源图、扩展表层子空间、表层协方差抑制和 EEG-MEG 交叉证据均未稳定超过 v3，因此没有并入正式算法。即使使用仿真真值给出的表层空间图，在代表性低/中/高 SNR 上混合深层 An_auc 也只有约 0.80，说明“所有低 SNR 混合情景均达到 0.90”不符合当前观测的信息上限。

## 最终冻结

- 全开发位置矩阵：188 套空间配置 × 49 个 EEG/MEG SNR 组合，共 9,212 例。
- manifest：`results/erp_whole_head/development_full_v3/manifest.json`
- SHA-256：`0989782ea769bc0a698e8e6f250795ee829398afc5984a904eeb9544a32432b5`
- 深源判定：全局相对幅值至少 0.14，且峰值距真值不超过 10 mm。0.14 是全开发集上保持敏感度不变且首次达到至少 0.90 特异度的最小阈值。
- DLE/SD 同时报条件值和 detection-aware 值；漏检时后者使用全头几何跨度惩罚。
- 下一步只运行空间位置完全不重叠的 `confirmation_v3`；确认结果不用于继续调参。
