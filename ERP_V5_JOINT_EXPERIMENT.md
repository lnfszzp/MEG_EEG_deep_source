# ERP-v5：浅深联合惩罚与数值收敛实验

2026-09-22。实验候选，不替换 v3/v4，不声称已解决真实数据或全头深源问题。

## 本轮改动

复用原仿真、7514 点网格、白化、模态权重和 An_auc/SD/DLE。四种场景、原种子和同一观测保持不变；没有固定空间高斯模板，也没有真值指导候选筛选。

1. 将统一的基线匹配滤波惩罚分成皮层与深层两组，分别按组内最大基线投影的 99% 分位校准。它是经验正则强度，不是显著性检验，不保证假阳性率。
2. 保留整个平滑时间子空间，而不是只保留联合观测中幅度最大的模式。61 个活动期样本使用 10 个正交 DCT 模式，来自 sigma=0.1 窗宽高斯频响 >=1% 的固定规则。该选择已看过开发波形保留率，不能称为独立盲验证，也不是空间模板。
3. 非平滑重加权求解增加可行对偶界、权重改变后的对偶投影、同权重续算和代理目标下降检查。默认最多 20 外轮，每轮 2000 步加最多两次续算；当前权重相对对偶间隙 <=0.001 且外迭代变化 <=0.01 才报告收敛。它不是非凸问题的全局最优证书。

保留原 M=I−0.5P 邻域创新坐标，联合求解所有浅/深位置。令 B=YQ、A=GM⁻¹（实际含增益归一化），Z=MX：

    F(Z) = 1/2 ||B−AZ||²
           + Σ_j λ_layer(j) a_j ε_a log(1+||Z_j||₂/ε_a)
           + β Σ_e ε_e log(1+||(DZ)_e||₂/ε_e)
    w_j ← ε_a/(ε_a+||Z_j||₂)
    v_e ← ε_e/(ε_e+||(DZ)_e||₂)

ε 在首个凸阶段后固定，之后每轮求相应加权源幅度/边差分凸子问题。深层无 TV 边，也不与皮层连边；当前深网格只有 16 个双侧丘脑点。

另一条 IRLS 消融将范数替换为 sqrt(||Z_j||²+δ²)，使用稀疏精度矩阵与传感器空间 Woodbury 精确求解每步二次上界。它是**不同的平滑目标**，没有精确稀疏零。虽然开发小例全部满足停止条件，但混合深源恢复差，未采用为默认候选。不能把“收敛更快”写成“定位更好”。

## 已完成诊断与边界

- 旧混合病例在 5/5 dB 下，深源时间波形仅保留约 10%–28%；统一阈值为深层独立阈值的约 3–5 倍。真值只用于诊断保留率，未送入逆解。
- `identifiability`：8 个既有病例的时间/噪声诊断，复现 v4 阈值。这里的图展示失败原因，不是 v5 效能。
- `ablation_irls_frozen_5_5`：4 场景 × 2 校准 × 2 时间基，16 次完整开发求解。
- `ablation_irls_no_mrf_5_5`：相同 16 次消融，取消固定邻域映射；整体结果差，保留而不隐藏。
- `ablation_admm_layer_smooth_5_5`：6 外轮的开发试算，含深源 3/3 检出，但纯浅源 1/1 深源假阳性，只有 1/4 达本次停止准则。这不是最终精度结果。
- `ablation_irls_5_5`：早期求解代码指纹改变而中止，无 completion，不纳入完整比较。

五 SNR × 四配置 × 八方法的更高精度复测正在运行，完成结果以 `pilot_five_snr_all_methods/completion.json` 和后续 REPORT 为准；不能把运行能力当作完成数量。9212 例全量开发矩阵尚未重跑。

## 入口

- `作者风格版/17-浅深源可辨识诊断.py`：只读原因诊断。
- `作者风格版/18-ERP浅深联合重加权仿真.py`：主复测，默认 20 病例、160 方法结果；按 #%% 顺序阅读。
- `作者风格版/19-浅深校准与时间基消融.py`：开发消融，所有设置显式记录。
- `candidates/oaster_balanced.py`：v5 联合反演与分层校准。
- `candidates/graph_reweight_solver.py`：非平滑求解和数值证书。
- `candidates/graph_irls.py`：保留的平滑目标消融和可运行自检。

仿真仍是相当于 40 次平均后的有色高斯噪声、同一前向生成/反演、固定 20–120 ms 活动期。不是实际保存的 40 个独立试次；没有据此检验 GFP 自选窗、跨头模型误差或真实数据泛化。下一步深源存在性需要独立试次/观测预测验证，而不是只根据深浅峰值比调整阈值。
