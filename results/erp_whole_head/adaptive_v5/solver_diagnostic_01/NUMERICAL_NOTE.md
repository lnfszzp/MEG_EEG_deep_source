Both original-rank and DCT8 cases finished before an attempted interruption;
no process was stopped and all completed results are retained in rows.csv.
The dual bound using only the returned sparse iterate was conservative. No
complete-convergence claim is made for these exploratory results.
The next version also evaluates the ADMM linear-iterate sensor residual as a
second independently feasible dual candidate, taking the better lower bound.
