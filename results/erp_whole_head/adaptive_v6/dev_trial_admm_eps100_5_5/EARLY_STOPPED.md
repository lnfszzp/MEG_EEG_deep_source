# Early stop: epsilon fraction 1.0, EEG/MEG 5/5 dB

Development only; calibration and validation were not read.

The first pure-surface case completed and converged, but H1
`An_cal_AUC=0.658673`, below the default 0.679861 for that case. The remaining
cases were not run because weakening the non-convex reweighting did not address
the low requested AUC.
