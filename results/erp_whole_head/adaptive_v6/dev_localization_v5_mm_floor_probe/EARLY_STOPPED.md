# v5 多轮 MM 权重下限探针：提前否决

这是一条有标签开发探针，不是正式校准或 validation。按照运行前冻结的条件，八个 H0/H1 拟合必须全部达到多轮固定点，且两个 H0 分数必须严格低于两个 H1 分数。

第一个病例 `case 00` 已使候选不可通过，因此停止剩余计算：

- 真值：纯表层，旧 outer=1 excess 分数 `1.444032`。
- 新 `[0.5, 1]` 权重多轮 MM excess 分数：`1.629518`，误报没有下降。
- H0：3 个外层记录，收敛，最终 stationarity gap `0`。
- H1：3 个外层记录后数值停滞；后两轮各用满 20 个内层预算，最终 stationarity gap `0.017323 > 0.001`，未收敛。
- H1 幅度权重范围 `[0.516204, 1]`，说明下限生效；失败不是参数未传入。

结论：单独把幅度和边缘权重限制在 `[0.5, 1]` 不能解决深浅层竞争，也不能保证数值固定点。根据预声明早停规则，不继续运行 case 02/04/10，不在该下限上做参数网格。
