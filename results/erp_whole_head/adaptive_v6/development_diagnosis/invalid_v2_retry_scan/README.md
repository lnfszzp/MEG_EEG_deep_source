# v2 失败校准求解器诊断

这组结果只用于开发期求解器诊断，不恢复或重用已经退役的 v2 正式校准。

- v2 的 6 个未收敛 H1 拟合中，病例 08、10、11、12、17 在 `max_inner_retries=6` 时收敛。
- 最难的病例 09 在 retry 6–9 时仍未收敛，full-model gap 依次为 0.003220、0.002406、0.001779、0.001307。
- 病例 09 在 `max_inner_retries=10` 时首次收敛：gap 0.00099958，容差 0.001，21700/22000 次迭代。
- 因此 v3 把统一上限冻结为 10。求解器达到容差即提前停止，所以这不会改变已经在更低上限内收敛的结果。

受保护 validation 没有在本诊断中读取或运行。
